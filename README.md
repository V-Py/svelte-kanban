# A simple kanban made with Svelte and TailwindCSS 🚀
🔨 Work in progress 🧰

## Installation ⚙️
```bash
npm not ready yes

# some comments
npm not ready either
```

# API

onCardModify(e.detail => {prop, col, card, value})
onCardSave(e.detail => {prop, col, card, value})